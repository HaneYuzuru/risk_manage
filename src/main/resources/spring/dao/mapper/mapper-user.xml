<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nju.risk.manage.dao.IUserDAO">
    <resultMap id="BaseResultMap" type="com.nju.risk.manage.domain.UserDO">
        <result column="id" property="id" jdbcType="INTEGER"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="type" property="userType" jdbcType="INTEGER"/>
        <result column="gmt_create" property="gmtCreate" jdbcType="TIMESTAMP"/>
        <result column="gmt_modified" property="gmtModified" jdbcType="TIMESTAMP"/>
        <result column="data_status" property="dataStatus" jdbcType="INTEGER"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,`name`,password,`type`,gmt_create,gmt_modified,data_status
    </sql>

    <update id="update" parameterType="com.nju.risk.manage.domain.UserDO">
        update `user`
        <set>
            gmt_modified = now(),
            <if test="name != null">
                name = #{name,jdbcType=VARCHAR},
            </if>
            <if test="password != null">
                password = #{password,jdbcType=VARCHAR},
            </if>
            <if test="userType != null">
                type = #{userType,jdbcType=INTEGER},
            </if>
            <if test="dataStatus != null">
                data_status = #{dataStatus,jdbcType=INTEGER},
            </if>
        </set>
        where id = #{id,jdbcType=INTEGER}
    </update>
    <update id="batchUpdate" parameterType="com.nju.risk.manage.domain.UserDO">
        <foreach collection="list" item="item" open="" close="" index="index" separator=";">
            update `user`
            <set>
                gmt_modified = now(),
                <if test="item.name != null">
                    name = #{item.name,jdbcType=VARCHAR},
                </if>
                <if test="item.password != null">
                    password = #{item.password,jdbcType=VARCHAR},
                </if>
                <if test="item.userType != null">
                    type = #{item.userType,jdbcType=INTEGER},
                </if>
                <if test="item.dataStatus != null">
                    data_status = #{item.dataStatus,jdbcType=INTEGER},
                </if>
            </set>
            <where>
                id = #{item.id,jdbcType=INTEGER}
            </where>
        </foreach>
    </update>

    <insert id="insert" parameterType="com.nju.risk.manage.domain.UserDO">
        <selectKey order="AFTER" resultType="integer" keyProperty="id">
            select last_insert_id() as id
        </selectKey>
        insert into `user`(<include refid="Base_Column_List"/>)
        values(
        #{id,jdbcType=INTEGER},
        #{name,jdbcType=VARCHAR},
        #{password,jdbcType=VARCHAR},
        #{userType,jdbcType=INTEGER},
        now(),
        now(),
        #{dataStatus,jdbcType=INTEGER})
    </insert>
    <insert id="batchInsert" parameterType="java.util.List">
        insert ignore into `user` (<include refid="Base_Column_List"/>)
        values
        <foreach collection="list" item="item" separator=",">
            (
            #{item.id,jdbcType=INTEGER},
            #{item.name,jdbcType=VARCHAR},
            #{item.password,jdbcType=VARCHAR},
            #{item.userType,jdbcType=INTEGER},
            now(),
            now(),
            #{item.dataStatus,jdbcType=INTEGER})
        </foreach>
    </insert>
    <update id="delete" parameterType="com.nju.risk.manage.domain.UserDO">
        update `user`
        <set>
            gmt_modified = now(),
            data_status = 1
        </set>
        where id = #{id,jdbcType=INTEGER}
    </update>
    <update id="batchDeleteByIdList" parameterType="java.util.List">
        update `user`
        <set>
            gmt_modified = now(),
            data_status = 1
        </set>
        <where>
            id in
            <foreach collection="list" open="(" close=")" separator="," item="item">
                #{item,jdbcType=INTEGER}
            </foreach>
        </where>
    </update>
    <select id="selectByIdList" resultMap="BaseResultMap" parameterType="java.util.List">
        select
        <include refid="Base_Column_List"/>
        from `user`
        WHERE data_status =0 AND
          id in
          <foreach collection="list" open="(" close=")" separator="," item="item">
              #{item,jdbcType=INTEGER}
          </foreach>
    </select>
    <select id="selectByName" resultMap="BaseResultMap" parameterType="java.lang.String">
        select
        <include refid="Base_Column_List"/>
        FROM `user`
        WHERE `name` = #{name} AND data_status =0
    </select>

    <select id="searchByName" resultMap="BaseResultMap" parameterType="java.lang.String">
        select
        <include refid="Base_Column_List"/>
        FROM `user`
        WHERE `name` LIKE '%${_parameter}%' AND data_status =0
    </select>

</mapper>
