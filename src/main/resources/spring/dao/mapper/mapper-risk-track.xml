<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nju.risk.manage.dao.IRiskTrackDAO">
    <resultMap id="BaseResultMap" type="com.nju.risk.manage.domain.RiskTrackDO">
        <result column="id" property="id" jdbcType="INTEGER"/>
        <result column="risk_id" property="riskId" jdbcType="INTEGER"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
        <result column="description" property="description" jdbcType="VARCHAR"/>
        <result column="gmt_create" property="gmtCreate" jdbcType="TIMESTAMP"/>
        <result column="gmt_modified" property="gmtModified" jdbcType="TIMESTAMP"/>
        <result column="data_status" property="dataStatus" jdbcType="INTEGER"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,risk_id,status,description,gmt_create,gmt_modified,data_status
    </sql>

    <insert id="insert" parameterType="com.nju.risk.manage.domain.RiskTrackDO" useGeneratedKeys="true" keyProperty="id">
        <selectKey order="AFTER" resultType="integer" keyProperty="id">
            select last_insert_id() as id
        </selectKey>
        insert into `risk_track`(<include refid="Base_Column_List"/>)
        values(
        #{id,jdbcType=INTEGER},
        #{riskId,jdbcType=INTEGER},
        #{status,jdbcType=INTEGER},
        #{description,jdbcType=VARCHAR},
        now(),
        now(),
        #{dataStatus,jdbcType=INTEGER})
    </insert>

    <insert id="batchInsert" parameterType="java.util.List">
        insert ignore into `risk_track` (<include refid="Base_Column_List"/>)
        values
        <foreach collection="list" item="item" separator=",">
            (
            #{item.id,jdbcType=INTEGER},
            #{item.riskId,jdbcType=INTEGER},
            #{item.status,jdbcType=INTEGER},
            #{item.description,jdbcType=VARCHAR},
            now(),
            now(),
            #{item.dataStatus,jdbcType=INTEGER})
        </foreach>
    </insert>

    <update id="update" parameterType="com.nju.risk.manage.domain.RiskTrackDO">
        update `risk_track`
        <set>
            gmt_modified = now(),
            <if test="riskId != null">
                risk_id = #{riskId,jdbcType=INTEGER},
            </if>
            <if test="status != null">
                status = #{status,jdbcType=INTEGER},
            </if>
            <if test="description != null">
                description = #{description,jdbcType=VARCHAR},
            </if>
            <if test="dataStatus != null">
                data_status = #{dataStatus,jdbcType=INTEGER},
            </if>
        </set>
        where id = #{id,jdbcType=INTEGER}
    </update>

    <update id="batchUpdate" parameterType="com.nju.risk.manage.domain.RiskTrackDO">
        <foreach collection="list" item="item" open="" close="" index="index" separator=";">
            update `risk_track`
            <set>
                gmt_modified = now(),
                <if test="item.riskId != null">
                    risk_id = #{item.riskId,jdbcType=INTEGER},
                </if>
                <if test="item.status != null">
                    status = #{item.status,jdbcType=INTEGER},
                </if>
                <if test="item.description != null">
                    description = #{item.description,jdbcType=VARCHAR},
                </if>
                <if test="item.dataStatus != null">
                    data_status = #{item.dataStatus,jdbcType=INTEGER},
                </if>
            </set>
            <where>
                id = #{item.id,jdbcType=INTEGER}
            </where>
        </foreach>
    </update>

    <update id="delete" parameterType="com.nju.risk.manage.domain.RiskTrackDO">
        update `risk_track`
        <set>
            gmt_modified = now(),
            data_status = 1
        </set>
        where id = #{id,jdbcType=INTEGER}
    </update>

    <update id="batchDeleteByIdList" parameterType="java.util.List">
        update `risk_track`
        <set>
            gmt_modified = now(),
            data_status = 1
        </set>
        <where>
            id in
            <foreach collection="list" open="(" close=")" separator="," item="item">
                #{item,jdbcType=INTEGER}
            </foreach>
        </where>
    </update>

    <select id="selectByIdList" resultMap="BaseResultMap" parameterType="java.util.List">
        select
        <include refid="Base_Column_List"/>
        from `risk_track`
        where data_status =0 AND
        id in
        <foreach collection="list" open="(" close=")" separator="," item="item">
            #{item,jdbcType=INTEGER}
        </foreach>
    </select>

    <select id="selectByRiskIdList" resultMap="BaseResultMap" parameterType="java.util.List">
        select
        <include refid="Base_Column_List"/>
        from `risk_track`
        where data_status =0 AND
        risk_id in
        <foreach collection="list" open="(" close=")" separator="," item="item">
            #{item,jdbcType=INTEGER}
        </foreach>
    </select>
</mapper>